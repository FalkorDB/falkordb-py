[tool.poetry]
name = "falkordb"
version = "0.1.0"
description = ""
authors = ["swilly22 <roi.lipman@falkordb.com>"]
readme = "README.md"
packages = [{include = "falkordb"}]

[tool.poetry.dependencies]
python = "^3.10"
redis = "^5.0.1"

[tool.poetry.group.test.dependencies]
pytest = "^7.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
